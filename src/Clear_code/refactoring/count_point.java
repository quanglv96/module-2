package Clear_code.refactoring;
import static Clear_code.refactoring.testTenisGame.score;

public class count_point {

    protected static int[] sumScore(int choice){
        if (choice==1){
            ;
        }else {
            score[1]+=1;
        }
        return score;
    }
}
